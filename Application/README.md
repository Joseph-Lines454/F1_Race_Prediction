# comp3006-assessment-template
IMPORTANT INSTRUCTIONS

When git Cloning project into new REPO - the file path of usr/src/app isnt found (dispite doing it) and express is not install (dispite doing it) on both backend server containers.
When veiwing the assigment please download the zip file because for some reason it works when downloading it as a ZIP (I have spent alot of time on this and dont udnerstand why).

When in Terminal of root folder with docker compose - use docker-compose up -d or docker-compose up -d --build.

Login to staff dashboard, username: staffmember and password: staffmember.
Thanks



It also contains a .gitignore file, which should prevent unnecessary (temporary) files being committed (specific to Node.js, which is required).
